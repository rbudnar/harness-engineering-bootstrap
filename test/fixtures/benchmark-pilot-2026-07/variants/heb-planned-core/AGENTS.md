# HEB Planned Core Fixture

Use the smallest task-specific route first. For this fixture, inspect `README.md`, run `npm test` for behavior changes, and avoid editing files that are not named by the task or grader.
