name: CI

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: pnpm --dir "services/api v2" run build
      - run: yarn --cwd "services/api v2" build
      - run: bun --cwd "services/api v2" run build
