name: CI

on:
  pull_request:

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - run: npx semantic-release
      - run: npm exec release-it
      - run: pnpm exec semantic-release
      - run: npm exec -- changeset publish
      - run: npx --yes changeset publish
      - run: semantic-release
