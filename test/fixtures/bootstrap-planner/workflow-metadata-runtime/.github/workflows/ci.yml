name: ci
on: [push]
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: docker/build-push-action@v6
        with:
          push: true
      - run: npm test
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
