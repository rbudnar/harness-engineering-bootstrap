{
  "name": "yarn-foreach-workspace-package-fixture",
  "private": true,
  "packageManager": "yarn@4.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -A run build"
  }
}
