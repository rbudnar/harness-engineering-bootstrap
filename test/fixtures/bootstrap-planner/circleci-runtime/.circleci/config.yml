version: 2.1

jobs:
  test:
    steps:
      - run:
          name: Test
          command: pytest
      - run:
          name: Test block
          command: |
            python -m pytest
  deploy:
    steps:
      - run: terraform apply -auto-approve
