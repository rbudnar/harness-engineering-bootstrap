name: CI

on:
  pull_request:

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - run: pnpm install
      - run: yarn install
      - run: npm ci --workspaces
      - run: npm ci --workspace web --workspace api
